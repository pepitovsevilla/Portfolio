@AGENTS.md

## Claude Code Notes

- Shared durable rules live in `AGENTS.md` so Codex and other agents follow the same guidance; keep the `@AGENTS.md` import on its own line.
- Don't duplicate `AGENTS.md` rules here — add durable project rules to `AGENTS.md` instead.
- For quick lookups, answer directly without todos. For multi-step work, plan before editing.
- Keep both files short to avoid token bloat.
